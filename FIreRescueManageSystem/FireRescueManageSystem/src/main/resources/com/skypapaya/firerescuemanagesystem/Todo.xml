<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skypapaya.firerescuemanagesystem.DAO.TodoDAO">

    <resultMap id="todoResultMap" type="com.skypapaya.firerescuemanagesystem.DO.TodoDO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="status" property="status"/>
        <result column="gmt_created" property="gmtCreated"/>
        <result column="gmt_modified" property="gmtModified"/>
        <result column="user_id" property="userId"/>
    </resultMap>

    <select id="findAllByUserId" resultMap="todoResultMap">
        SELECT * FROM todo
        WHERE user_id = #{userId}  -- 新增条件
        ORDER BY gmt_created DESC
    </select>

    <insert id="insertTodo" parameterType="com.skypapaya.firerescuemanagesystem.DO.TodoDO"
            useGeneratedKeys="true" keyProperty="id">

        INSERT INTO todo (title, status, gmt_created, gmt_modified, user_id)
        VALUES (#{title}, #{status}, now(), now(), #{userId})

    </insert>

    <update id="updateTodoStatus" parameterType="com.skypapaya.firerescuemanagesystem.DO.TodoDO">
        UPDATE todo
        SET status = #{status}, gmt_modified = now()
        WHERE id = #{id} AND user_id = #{userId} -- 新增 user_id 条件
    </update>

    <delete id="deleteTodo">
        DELETE FROM todo
        WHERE id = #{id} AND user_id = #{userId} -- 新增 user_id 条件
    </delete>

    <update id="updateAllStatus">
        UPDATE todo
        SET status = #{status}, gmt_modified = now()
        WHERE status != #{status} AND user_id = #{userId} -- 新增 user_id 条件
    </update>

    <delete id="deleteAllByUserId">
        DELETE FROM todo WHERE user_id = #{userId}
    </delete>
</mapper>